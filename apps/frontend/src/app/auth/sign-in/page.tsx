import SignIn from "@/components/forms/authentication/sign-in"

export default function SignInPage() {
    return (
        <div className="min-h-screen flex items-center justify-center">
            <SignIn />
        </div>
    );
}
